rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all read/write access from client-side SDKs
    // Admin Portal uses Admin SDK which bypasses these rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
