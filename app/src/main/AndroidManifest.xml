<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    
    <!-- Required for Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartExamSA">
        
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartExamSA.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activities.QuestionFormActivity"
            android:exported="false"
            android:label="Add Question" />

        <activity
            android:name=".activities.AssessmentGeneratorActivity"
            android:exported="false"
            android:label="Generate Assessment" />

        <activity
            android:name=".activities.MarketplaceActivity"
            android:exported="false"
            android:label="Question Pack Store" />
            
        <activity
            android:name=".marketplace.ui.PackDetailActivity"
            android:exported="false"
            android.label="Pack Details" />

        <activity
            android:name=".activities.QuestionBankActivity"
            android:exported="false"
            android:label="Question Bank" />

        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false"
            android:label="Teacher Settings" />

        <activity
            android:name=".activities.TermsAcceptanceActivity"
            android:exported="false"
            android:label="Terms & Conditions"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.TrialWelcomeActivity"
            android:exported="false"
            android:label="Welcome" />

        <activity
            android:name=".activities.RegistrationActivity"
            android:exported="false"
            android:label="Create Account"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.PaperDetailActivity"
            android:exported="false"
            android:label="Paper Preview" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".marketplace.services.SyncService"
            android.permission="android.permission.BIND_JOB_SERVICE" />

    </application>

</manifest>
